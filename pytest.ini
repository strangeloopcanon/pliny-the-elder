[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    anyio: run async tests with anyio/asyncio event loop
    asyncio: legacy marker; prefer anyio
    timeout: per-test timeout (requires pytest-timeout if enforcement needed)
